{
  "name": "sga-check-overflow",
  "private": true,
  "description": "Headless overflow sweep for the root viewer pages (see check_overflow.sh)",
  "dependencies": {
    "puppeteer": "^24.0.0"
  }
}
